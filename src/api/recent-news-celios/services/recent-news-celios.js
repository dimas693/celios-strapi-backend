'use strict';

/**
 * recent-news-celios service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::recent-news-celios.recent-news-celios');

'use strict';

/**
 * recent-news-celios router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::recent-news-celios.recent-news-celios');

'use strict';

/**
 * trainings-celios controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::trainings-celios.trainings-celios');

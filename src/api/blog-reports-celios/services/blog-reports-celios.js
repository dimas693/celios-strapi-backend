'use strict';

/**
 * blog-reports-celios service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::blog-reports-celios.blog-reports-celios');

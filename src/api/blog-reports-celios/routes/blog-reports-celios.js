'use strict';

/**
 * blog-reports-celios router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::blog-reports-celios.blog-reports-celios');
